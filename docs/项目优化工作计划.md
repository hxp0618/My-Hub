# My Hub 项目优化工作计划

## 目标

把 My Hub 从功能完整的浏览器工作台，推进到更稳定、可维护、可发布的扩展产品。第一阶段优先处理质量闸门、隐私安全、类型稳定性和基础体验一致性；第二阶段再扩展更有差异化的新功能。

## 第一阶段：工程质量与发布可信度

- 补齐 `typecheck`、`lint`、`test`、`build` 四类本地质量检查，并让它们可以纳入 CI。
- 修复当前 TypeScript 类型断点，重点覆盖全局搜索结果、LLM 消息类型、工具动态导入、React ref、第三方库声明。
- 修复 Vitest 测试环境的 localStorage 问题，稳定属性测试结果。
- 迁移 ESLint 9 flat config，恢复项目文档中要求的 lint 工作流。
- 减少生产日志，避免在后台脚本和 AI 整理流程里输出 URL、通知内容、密钥路径等敏感信息。

## 第二阶段：隐私、安全与权限收敛

- 将导入导出拆成“普通数据”和“敏感数据”两档，默认不导出 LLM Key、Bark Key、通知历史。
- 为敏感配置增加脱敏预览和二次确认。
- 将 `<all_urls>` 内容脚本改为按需注入或最小匹配范围，减少商店审核风险。
- 梳理 `clipboardRead`、`tabs`、`sessions` 等权限的实际使用入口，在设置页补充权限说明。

## 第三阶段：架构拆分

- 拆分 `src/pages/background/index.ts`：书签同步、Bark 定时任务、订阅提醒分别独立模块。
- 拆分 `BookmarkPage.tsx`：页面容器、工具栏、弹窗、拖拽排序、批量操作各自成模块。
- 将 localStorage、IndexedDB、chrome.storage.local 封装为统一 repository，统一迁移、校验和导入导出。
- 把工具箱改为 manifest 注册机制，减少新增工具时需要同时修改 enum、metadata、loader、i18n 的重复工作。

## 第四阶段：UI/UX 与主题一致性

- 清理旧 `.btn-*` 样式，统一到 `nb-*` 组件令牌。
- 将输入框 focus 从蓝色 glow 改为 Neo-Brutalism 硬阴影。
- 将主题元数据、空状态、错误提示全部纳入 i18n。
- 为移动或窄屏增加侧边栏折叠逻辑，避免固定宽度影响可用性。

## 第五阶段：新功能建议

- 命令中心：在统一搜索里支持 `tool:`、`tag:`、`url:`、`action:` 前缀，快速打开工具、搜索标签、执行批量动作。
- 书签健康报告：检测重复、失效、无标签、长期未访问、可归档项目，并给出一键整理建议。
- AI 整理预演：展示 diff、风险提示、置信度、撤销快照，支持只应用选中的整理项。
- 订阅到期日历：按月历/时间轴展示订阅，统计月/年支出，并联动 Bark 与浏览器通知。
- 工具工作流：允许把多个开发者工具串成流水线，例如 URL 解码 -> JSON 格式化 -> Diff。

## 本轮执行范围

- 新增工程脚本：`typecheck`、`lint`。
- 增加 ESLint flat config。
- 修复当前测试环境 localStorage 问题。
- 修复当前 `tsc --noEmit` 暴露的类型问题。
- 修复英文 i18n 缺失项。
- 移除新标签页重复 ThemeProvider。
- 完成 `test`、`typecheck`、`lint`、`build:chrome` 验证，并用 Chrome 做基础页面校验。

## 继续优化记录

- 将 ESLint 告警从 100 个清理到 0 个，优先移除未使用导入、死变量、无意义返回值和可安全修正的 Hook 依赖。
- 收敛工具页、Bark 通知、二维码、订阅、格式转换测试、服务层中的历史遗留声明。
- 为添加书签自动建议保留初始化触发语义，并用少量注释说明避免重复发起 AI 请求的原因。
- 复跑 `typecheck`、`test`、`lint`、`build:chrome`，自动化验证均通过。
- 已使用用户当前 Google Chrome 的 Codex 插件/Computer Use 环境完成真实浏览器校验：`My Hub` 成功作为新标签页加载，首页、工具页、工具搜索、Bark 通知工具、设置弹窗、通用设置、主题/亮度/语言区域均可正常渲染。

## 隐私日志收敛记录

- 将后台脚本中的调试日志切换为开发环境输出，生产环境不再打印书签 URL、任务调度细节和 Bark 通知完整 URL。
- 移除 AI 整理相关的 LLM 原始响应、prompt、书签树、批量整理数据在生产日志中的输出。
- 清理添加书签、AI 整理确认弹窗、整理进度弹窗、内容脚本中的调试日志，避免在用户浏览页面时产生无意义控制台噪音。
- Bark 通知发送日志只保留开发环境下的 key id/label 摘要，不输出设备密钥、标题、正文或 query 参数。

## 权限收敛记录

- 移除 `sessions` 权限：当前代码没有使用 `chrome.sessions`，历史页只读取本机 `chrome.history` 并显示 Current Device。
- 将内容脚本匹配范围从 `<all_urls>` 收敛为 `http://*/*` 和 `https://*/*`，避免注入 file、chrome-extension 等更敏感页面。
- 保留 `clipboardRead`、`activeTab`、`tabs`：当前添加书签、打开标签详情、首页剪贴板读取等入口仍在使用。
- 设置页新增“权限说明”区域，逐项说明 bookmarks、history、storage、tabs/activeTab、clipboardRead、alarms 和 http/https 内容脚本的用途与范围。
- 已确认 `dist_chrome/manifest.json` 中权限列表和 content script matches 与预期一致。
- 复跑 `lint`、`typecheck`、`test`、`build:chrome`，自动化验证均通过。

## 敏感数据导出优化记录

- 全局数据导出新增默认安全模式：默认清空 LLM API Key、各服务商 Key、Bark 设备密钥、订阅通知渠道密钥和 Bark 发送历史；脱敏后的通知渠道会转为禁用，避免导入后产生无效发送配置。
- 设置页数据管理区域新增“包含敏感配置”开关，默认关闭，并用绿色提示说明安全导出行为。
- 用户显式开启完整备份时，界面显示风险提示；点击导出前会弹出二次确认，避免误导出可用密钥。
- 为 LLM、Bark、订阅通知配置的脱敏逻辑补充单元测试，覆盖密钥清空、通知渠道禁用和“不修改当前浏览器配置”的回归要求。
- 导入安全导出的备份时，空密钥不会覆盖本机已有 LLM Key、Bark Key 和订阅通知密钥；完整导出中显式包含的密钥仍会按导入文件恢复。
- 无可用设备密钥的 Bark 脱敏条目会在导入时跳过，避免生成选中但不可发送的空 Key。
- 已在真实 Google Chrome 中校验：新设置项、默认安全提示、敏感模式风险提示和二次确认弹窗均可正常显示。
- 复跑 `lint`、`typecheck`、`test`、`build:chrome`，自动化验证均通过；当前测试数为 4 个测试文件、68 个测试。

## Prompt API 兼容性优化记录

- 为 Gemini Nano / Chrome Prompt API 会话增加 `expectedOutputs` 输出语言声明，避免 Chrome 扩展错误页出现缺少 output language 的运行时警告。
- 根据当前界面语言映射 Prompt API 支持的输出语言；Chrome 当前仅支持 `en`、`es`、`ja`，其他语言回退到 `en`，避免声明不支持的语言导致会话创建失败。
- 收紧 `LanguageModel` 全局类型声明，让 `availability` 和 `create` 使用同一组选项类型。
- LLM 服务日志中的 API Key 已脱敏，即使开发环境打开日志，也不会直接输出可用密钥。
- 复跑 `lint`、`typecheck`、`test`、`build:chrome`，自动化验证均通过。

## UI/主题一致性优化记录

- 将旧 `.btn-primary` 和 `.btn-secondary` 兼容样式对齐到 Neo-Brutalism 令牌：统一 2px 边框、硬阴影、按压位移动效和主题变量颜色。
- 将全局 `input`、`textarea`、`select` 的 focus 状态从蓝色发光阴影改为硬阴影，避免非 `nb-input` 控件在深色/护眼主题下出现旧视觉。
- 清理新标签页里未被引用的 `.btn-capsule` 旧类，减少后续组件误用圆形胶囊按钮样式的概率。
- 已使用 Google Chrome 校验扩展新标签页可正常重载，首页搜索框可获得焦点，设置页仍可正常打开；同时确认生产 CSS 不再包含旧 `0 0 0 3px rgba(59, 130, 246, 0.1)` focus glow。
- 复跑 `lint`、`typecheck`、`test`、`build:chrome`，自动化验证均通过；当前测试数为 4 个测试文件、68 个测试。

## CI 质量闸门优化记录

- 将 `.github/workflows/ci.yml` 从手动 Chrome 构建升级为 PR/push 自动质量检查，覆盖 `lint`、`typecheck`、`test`、`build:chrome`。
- 将 CI 运行环境从 Node 18 + yarn 调整为 Node 22 + `npm ci`，与当前 Vite 7 和 `package-lock.json` 的实际项目形态保持一致。
- 将 `actions/setup-node` 和 `actions/upload-artifact` 升级到 v4，减少 GitHub Actions 运行时弃用风险。
- 已用本地 `yaml` 解析器校验 workflow 语法通过。
- 本地验证 `build:firefox` 通过后，将 Firefox 生产构建和 `dist_firefox` artifact 上传纳入同一个 CI workflow，补齐双浏览器发布检查。

## 生产日志继续收敛记录

- 将书签 AI 整理落地流程中的直接 `console.log/warn/error` 改为开发环境 logger，生产环境不再输出生成的书签树、文件夹标题、书签 ID 和父级 ID。
- 将书签 HTML 导出、Bark 旧配置迁移、Bark 定时任务 alarm 注册/取消日志切换到开发环境输出，避免发布包里暴露任务 ID、调度时间或迁移细节。
- 将首页剪贴板读取失败日志切换到开发环境 logger，避免权限未授予时在生产控制台反复输出。
- 清理 LLM 服务中遗留的注释掉的 `console.log` 调试代码；当前源码中仅保留 `src/utils/logger.ts` 内部用于开发环境 debug 的 `console.log`。

## 命令中心前缀搜索记录

- 全局搜索新增 `tool:` 前缀：例如 `tool:json`、`tool:qr` 会直接搜索工具元数据，匹配工具 ID、名称、描述和分类。
- 全局搜索继续补齐 `tag:` 和 `url:` 前缀：`tag:ai` 只搜索带有匹配标签的书签，`url:github` 只按 URL 字段搜索历史和书签；`tag:` 空查询会列出已打标签的书签。
- 全局搜索新增安全的 `action:` 前缀，当前只注册无副作用的页面导航和设置入口，例如 `action:settings`、`action:bookmarks`、`action:history`，避免把删除或批量修改类操作暴露为命令。
- 首页搜索结果新增工具结果卡片，展示工具图标、名称、描述和分类；点击后切换到工具页并选中目标工具。
- 首页搜索结果新增操作结果卡片，使用蓝色 Neo-Brutalism 图标块区分导航动作，点击后打开对应页面或设置弹窗。
- 工具页新增外部初始工具参数，保留原本最近使用工具和启用工具配置逻辑；如果 `tool:` 指向已启用工具，会自动打开对应工具。
- 搜索占位文案已同步中英文 i18n，提示用户可以输入 `tool:json`、`tag:ai`、`url:github` 和 `action:settings`。
- 自动化验证已覆盖 `lint`、`typecheck`、`test`、`build:chrome` 和 `build:firefox`。
- 当前真实 Google Chrome 仍显示旧扩展实例的搜索占位文案，说明浏览器中的 My Hub 尚未重新加载最新 `dist_chrome`；受 Chrome 内部页面自动化限制，需要在 `chrome://extensions/` 手动点击 My Hub 的重新加载按钮后，再复测 `tool:json` UI 流程。

## Favicon 稳定性优化记录

- 新增统一 `favicon` 工具函数，集中处理域名解析和 Google favicon URL 生成。
- 首页、历史页、书签页、标签详情、历史时刻推荐和全局搜索已统一使用安全解析，避免 `chrome://`、`about:blank`、临时 URL 或异常字符串触发 `new URL(...).hostname` 崩溃。
- 异常 URL 会使用内置 SVG favicon 兜底，避免引用不存在的 `/default-favicon.png` 资源。
- 为标准 URL、异常 URL 和浏览器内部 URL 补充单元测试；当前测试数更新为 5 个测试文件、71 个测试。

## 书签健康报告记录

- 新增书签健康分析工具，统计全量书签中的重复项、无标签、长期未访问和非 http/https 异常 URL。
- 书签页新增健康概览区域，使用 Neo-Brutalism 卡片展示健康分、重复项、无标签、长期未访问和异常 URL，帮助用户快速判断整理优先级。
- 健康问题卡片可点击筛选全量书签；搜索框输入会自动退出健康筛选，头部提供“清除筛选”入口。
- 健康分仅作为提示，不自动删除或移动书签；真正有副作用的去重仍保留原有确认弹窗流程。
- 为健康分析补充单元测试，覆盖重复、无标签、长期未访问、异常 URL、健康筛选匹配和空书签树。

## 订阅到期日历记录

- 新增订阅到期日历工具，按当前月起未来 6 个月聚合订阅，并统计已过期、停用和即将到期数量。
- 订阅页新增概览卡片：已过期、本月到期、30 天内到期、启用中，帮助快速判断续费优先级。
- 月份卡片支持点击筛选订阅列表，筛选状态可一键清除；空月份保持低强调展示，避免误导用户进入空列表。
- 日历和筛选文案已同步中英文 i18n，UI 使用现有 Neo-Brutalism 卡片、徽章和硬阴影风格。
- 为月份分组、摘要统计和月份筛选补充单元测试，覆盖日期边界和停用订阅计数。
- 订阅列表的到期日期和剩余天数显示已改为按当前界面语言格式化，不再固定使用中文日期和“天/今天”硬编码。
- 订阅表单校验文案细化：自定义类型和到期日期不再复用“请输入订阅名称”；错误边框改用 Neo-Brutalism 的粉色变量。
- 月份筛选后的空状态使用专门文案，避免用户误以为当前没有任何订阅数据。
- 订阅表单日期输入改为本地日期解析/格式化，避免 `YYYY-MM-DD` 经 UTC 解析或 `toISOString()` 回填时在不同时区出现错一天。
- 修复订阅状态边界：到期日当天保持“正常/即将到期”，不再因为到期时间戳是当天 00:00 而在白天被误判为已过期。
- 后台订阅提醒复用服务层剩余天数逻辑，避免前台列表和后台通知对“今天到期”的判断再次分叉。
- 订阅通知日期格式改为跟随用户/浏览器语言，不再在后台和通知服务中固定使用 `zh-CN`。
- 订阅通知标题/正文抽成共享 formatter，后台与通知服务统一使用同一套中英文文案；英文界面不再收到中文到期提醒正文。
- 订阅通知渠道测试内容也改为共享 formatter，测试 Telegram、Email、Webhook、Bark 时会按用户语言生成测试标题、正文和订阅名称。
- 订阅通知发送失败 fallback 文案支持中英文，包括配置不完整、未启用、Bark Key 缺失、发送失败和网络错误。
- 订阅通知发送渠道选择抽成共享逻辑，`sendReminder` 与后台提醒都只会发送“订阅已选择且全局已启用”的渠道，避免订阅只选 Email 时误发到 Telegram/Bark。

## HTTP 工具 curl 导入记录

- curl 导入解析失败不再返回固定中文文案，改为稳定错误码，由导入弹窗按当前语言映射为中英文提示。
- 补充 curl 解析/生成单元测试，覆盖成功解析、空命令、非 curl 前缀、不支持的 HTTP 方法、缺少 URL 和 shell 参数转义。

## 订阅操作反馈记录

- 订阅页新增操作 Toast 反馈，添加、编辑、删除、续订、启用/停用和设置保存会给出成功或失败提示，不再只在控制台记录异常。
- 服务层订阅错误码新增 UI 映射工具，将无效名称、订阅不存在、一次性订阅续订、存储错误等情况转为中英文 i18n 文案。
- 为订阅错误码到 i18n key 的映射补充单元测试，避免后续新增错误码时退回到硬编码中文。

## 订阅备份恢复记录

- 订阅设置新增“备份与恢复”入口，支持导出订阅、通知配置和提醒设置为 JSON 文件。
- 导入备份采用合并模式并增加确认弹窗：同名订阅会跳过，备份中的通知配置和提醒设置会应用到当前环境，避免无提示覆盖。
- 导入完成后通过 Toast 展示新增和跳过数量；导出/导入失败使用中英文 i18n 文案提示。
- 为订阅备份数据校验补充单元测试，覆盖有效备份、非法 JSON 和无效订阅字段。
- 订阅备份默认脱敏通知渠道密钥，导入脱敏备份时保留当前浏览器已有通知配置，避免安全备份反向清空有效密钥。
- 订阅通知配置脱敏/合并逻辑抽为共享工具，数据同步和订阅备份使用同一套隐私规则，并补充隐私工具单元测试。
- 订阅备份导入校验新增结构化 issue，导入失败 Toast 会展示当前语言下的具体失败原因，例如非法 JSON、缺少订阅数据或第 N 个订阅字段无效。
- 选择订阅备份文件时先做本地预检，非法备份会直接提示具体原因，不再进入确认弹窗后才失败。
